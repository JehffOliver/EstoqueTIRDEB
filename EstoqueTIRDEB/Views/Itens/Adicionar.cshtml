@model IEnumerable<EstoqueTIRDEB.Models.Itens>

@{
    ViewData["Title"] = "Itens por Categoria";
    Layout = "_Layout";
}

<h2>Itens por Categoria</h2>

<table class="table">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Modelo</th>
            <th>Especificações</th>
            <th>Data de Aquisição</th>
            <th>Quantidade</th>
            <th>Categoria</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nome</td>
                <td>@item.Modelo</td>
                <td>@item.Especificações</td>
                <td>@item.DataAquisicao</td>
                <td>@item.Quantidade</td>
                <td>@item.Categoria</td>
                <td>
                    <form asp-controller="Itens" asp-action="AdicionarRemover" method="post">
                        <input type="hidden" name="itemId" value="@item.Id" />
                        <button type="submit" class="btn btn-primary">Remover e Adicionar</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>